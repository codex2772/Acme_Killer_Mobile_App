name: acme_killer_mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  csv: ^7.2.0
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.4
  get: ^4.7.3
  mobile_scanner: ^7.2.0
  particles_flutter: ^0.1.4
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/login/
